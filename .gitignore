# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# Test caches
.pytest_cache/
.hypothesis/

# Node / Astro
node_modules/
template/dist/
.astro/

# Puriq build artifacts
.puriq-build/

# Contratos generados al correr el wizard/CLI en el cwd. Se ignoran SOLO en la
# raiz del repo y en agent/ (donde se ejecutan las herramientas durante el
# desarrollo). Los `/` iniciales y la ruta explicita evitan pisar los contratos
# legitimos de `template/src/data/` (los usa la plantilla) y de `examples/`.
/tourism-data.json
/site.config.json
/theme.tokens.json
/assets/
/content/
/dist/
agent/tourism-data.json
agent/site.config.json
agent/theme.tokens.json
agent/assets/
agent/content/
agent/dist/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/
.idea/

# FUSE temp files (mount)
.fuse_hidden*
